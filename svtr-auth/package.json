{
  "name": "svtr-auth",
  "version": "1.0.0",
  "description": "SVTR Authentication Service - Email OTP + Magic Link",
  "main": "worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler dev --local"
  },
  "dependencies": {
    "hono": "^3.12.0",
    "aws4fetch": "^1.0.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.0.0"
  },
  "keywords": ["cloudflare", "workers", "authentication", "aws-ses", "otp", "magic-link"],
  "author": "SVTR Team",
  "license": "MIT"
}